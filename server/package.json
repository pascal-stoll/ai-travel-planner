{
  "name": "travelmind-backend",
  "version": "1.0.0",
  "description": "Backend API for TravelMind AI travel planner",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "echo \"Error: no linter specified\" && exit 1",
    "clean": "rm -rf node_modules package-lock.json && npm install",
    "health": "curl -s http://localhost:3001/api/health | jq ."
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "axios": "^1.6.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "joi": "^18.2.1",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
